"use client";
import { useEffect,useState } from "react";import { ArrowRight,CheckCircle2,Clock3,GitCommit,MoreHorizontal,Sparkles } from "lucide-react";import { getDashboardMetrics,getPlatformHealth,getReleases } from "@/lib/api/dashboard";import type { Health,Metric,Release } from "@/types";import { MetricCard } from "./metric-card";import { useWorkspaceContext } from "@/workspace-context";
const throughput=[48,62,57,75,69,86,93];const stages=[["Ingest","48.2K","99.6%"],["Normalize","47.9K","99.4%"],["Parse","47.6K","99.1%"],["Extract","46.8K","98.3%"],["Enrich","46.7K","99.8%"],["Index","46.5K","99.5%"],["Publish","24","100%"]];
export function Dashboard(){const [metrics,setMetrics]=useState<Metric[]>([]);const [health,setHealth]=useState<Health[]>([]);const [releases,setReleases]=useState<Release[]>([]);const release=useWorkspaceContext(s=>s.release?.name||"All releases"),project=useWorkspaceContext(s=>s.project?.name||"the selected Project");useEffect(()=>{setMetrics([]);Promise.all([getDashboardMetrics(),getPlatformHealth(),getReleases()]).then(([m,h,r])=>{setMetrics(m);setHealth(h);setReleases(r)})},[release]);return <main className="dashboard"><div className="page-heading"><div><span className="eyebrow">PLATFORM OVERVIEW</span><h1>Good morning, Alex</h1><p>Here’s what’s happening across <b>{project}</b>, release <b>{release}</b>.</p></div><button><Sparkles/> Ask ReleaseLens</button></div><section className="metric-grid">{metrics.length?metrics.map(m=><MetricCard key={m.label} metric={m}/>):Array.from({length:8},(_,i)=><div key={i} className="metric-skeleton skeleton"/>)}</section><div className="dashboard-grid"><section className="card pipeline-card"><div className="card-head"><div><h2>Knowledge pipeline</h2><p>Artifact throughput and processing health</p></div><span className="healthy"><i/> Live</span></div><div className="pipeline-stages">{stages.map((s,i)=><div key={s[0]}><span>{i+1}</span><b>{s[0]}</b><strong>{s[1]}</strong><small>{s[2]} success</small>{i<6&&<ArrowRight/>}</div>)}</div><div className="chart-title"><span><b>7-day throughput</b><small>Artifacts processed</small></span><strong>48,240 <small>+14.2%</small></strong></div><div className="line-chart">{throughput.map((v,i)=><div key={i}><span style={{height:`${v}%`}}/><small>{["Fri","Sat","Sun","Mon","Tue","Wed","Thu"][i]}</small></div>)}</div></section><section className="card health-card"><div className="card-head"><div><h2>Platform health</h2><p>Live service status</p></div><button>View details</button></div>{health.length?health.map(h=><div className="health-row" key={h.name}><i className={h.status.toLowerCase()}/><span><b>{h.name}</b><small>{h.latency} latency</small></span><em>{h.status}</em><small>{h.uptime}</small></div>):<div className="health-load skeleton"/>}</section><section className="card releases-card"><div className="card-head"><div><h2>Recent release snapshots</h2><p>Latest immutable knowledge packages</p></div><button>View all <ArrowRight/></button></div><div className="table-wrap"><table><thead><tr><th>VERSION</th><th>STATUS</th><th>BUILD</th><th>COMMIT</th><th>PUBLISHED</th><th>OWNER</th><th/></tr></thead><tbody>{releases.map(r=><tr key={r.version}><td><b>v{r.version}</b></td><td><span className={`release-status ${r.status.toLowerCase()}`}><i/>{r.status}</span></td><td>{r.build}</td><td><code><GitCommit/> {r.commit}</code></td><td>{r.date}</td><td>{r.author}</td><td><MoreHorizontal/></td></tr>)}</tbody></table></div></section><section className="card assistant-card"><div className="assistant-glow"/><Sparkles/><span>RELEASELENS AI</span><h2>Ask about any release</h2><p>Get grounded answers backed by release-scoped evidence and inline citations.</p><div className="suggestions"><button>What changed in {release}?</button><button>Which APIs have breaking changes?</button><button>Show release readiness risks</button></div><button className="start">Start a conversation <ArrowRight/></button><div className="confidence"><CheckCircle2/><span><b>98.7% citation accuracy</b><small>Validated across 1,240 answers</small></span></div></section><section className="card activity-card"><div className="card-head"><div><h2>Recent activity</h2><p>Latest platform events</p></div><button>View all</button></div>{[["Release 3.2.1 published","Immutable snapshot signed","green"],["GitHub sync completed","1,284 artifacts processed","blue"],["Guardrail blocked response","Cross-release evidence detected","red"],["Pipeline extraction delayed","Confluence rate limit","orange"]].map((a,i)=><div className="activity" key={a[0]}><i className={a[2]}/><span><b>{a[0]}</b><small>{a[1]}</small></span><em><Clock3/>{i*12+4}m</em></div>)}</section></div></main>}
