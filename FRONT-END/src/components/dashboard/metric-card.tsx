import type { Metric } from "@/types";import { ArrowDownRight,ArrowUpRight } from "lucide-react";
export function MetricCard({metric}:{metric:Metric}){const down=metric.change.startsWith("-");return <article className={`metric-card card ${metric.tone}`}><div><small>{metric.label}</small><span className={down?"down":"up"}>{down?<ArrowDownRight/>:<ArrowUpRight/>}{metric.change}</span></div><strong>{metric.value}</strong><p>{metric.description}</p><svg viewBox="0 0 140 32" preserveAspectRatio="none"><polyline points={metric.trend.map((v,i)=>`${i*23},${32-v/3.3}`).join(" ")}/></svg></article>}
